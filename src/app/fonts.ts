import { Alata, Pacifico } from 'next/font/google';

export const alata = Alata({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});

export const pacifico = Pacifico({
  weight: "400",
  subsets: ["latin"],
  display: "swap",
});
